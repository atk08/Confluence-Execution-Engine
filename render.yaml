services:

  - type: web

    name: confluence-execution-engine

    runtime: python

    buildCommand: pip install -r requirements.txt

    startCommand: uvicorn backend.app:app --host 0.0.0.0 --port 10000

    envVars:

      - key: TWELVE_DATA_API_KEY

        sync: false